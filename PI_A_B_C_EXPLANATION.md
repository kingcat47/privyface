# pi_a, pi_b, pi_c 상세 설명

## pi_a, pi_b, pi_c란?

`pi_a`, `pi_b`, `pi_c`는 **Groth16 ZK-SNARK 프로토콜**에서 사용하는 **타원곡선 위의 점들**입니다. 이 세 점의 조합이 "모든 제약 조건이 만족되었다"는 수학적 증명입니다.

## 각각의 의미

### pi_a (π_A) - 첫 번째 증명 조각

**수학적 의미:**

- **G₁ 그룹**의 점 (타원곡선 위의 좌표)
- 증명 생성 시 **회로의 입력값들과 다항식의 관계**를 타원곡선 점으로 변환한 첫 번째 조각

**역할:**

- Private Input (비공개 입력값)과 Public Input (공개 입력값)의 관계를 암호화
- "나는 올바른 Private Input을 알고 있다"는 첫 번째 증거

**데이터 형식:**

```typescript
pi_a: [string, string, string]; // [x, y, z] 좌표 (3개)
```

**예시:**

```json
{
  "pi_a": [
    "1234567890123456789012345678901234567890...", // x 좌표
    "9876543210987654321098765432109876543210...", // y 좌표
    "1" // z 좌표 (일반적으로 1)
  ]
}
```

### pi_b (π_B) - 두 번째 증명 조각

**수학적 의미:**

- **G₂ 그룹**의 점 (복소수 평면 표현)
- 증명 생성 시 **다항식의 복잡한 연산 과정**을 검증하기 위한 두 번째 조각

**역할:**

- 회로 내부의 **모든 연산 과정**을 암호화
- "나는 모든 연산을 올바르게 수행했다"는 두 번째 증거
- G₂ 평면을 사용하여 **보안성 향상** (G₁과 다른 그룹 사용)

**데이터 형식:**

```typescript
pi_b: [
  [string, string],
  [string, string],
  [string, string],
]; // 2x2 배열
```

**예시:**

```json
{
  "pi_b": [
    [
      "1111111111111111111111111111111111111111...",
      "2222222222222222222222222222222222222222..."
    ], // 첫 번째 점
    [
      "3333333333333333333333333333333333333333...",
      "4444444444444444444444444444444444444444..."
    ], // 두 번째 점
    [
      "5555555555555555555555555555555555555555...",
      "6666666666666666666666666666666666666666..."
    ] // 세 번째 점
  ]
}
```

### pi_c (π_C) - 세 번째 증명 조각

**수학적 의미:**

- **G₁ 그룹**의 점 (타원곡선 위의 좌표)
- 입력값과 연산 과정이 서로 일치함을 최종적으로 묶어주는 세 번째 조각

**역할:**

- pi_a와 pi_b를 **결합**하여 최종 증명 완성
- "입력값과 연산 과정이 서로 일치한다"는 최종 증거
- **일관성 검증**: 모든 것이 서로 일치하는지 확인

**데이터 형식:**

```typescript
pi_c: [string, string, string]; // [x, y, z] 좌표 (3개)
```

**예시:**

```json
{
  "pi_c": [
    "7777777777777777777777777777777777777777...", // x 좌표
    "8888888888888888888888888888888888888888...", // y 좌표
    "1" // z 좌표 (일반적으로 1)
  ]
}
```

## 왜 Proof에 들어가야 하는가?

### 1. 수학적 증명의 필요성

ZK-SNARK는 **"나는 조건을 만족하는 Private Input을 알고 있다"**는 것을 증명해야 합니다.

하지만 Private Input을 직접 공개하면:

- 익명성 파괴 (정부 DB 특징값, 카메라 특징값 노출)
- 프라이버시 침해

따라서 **수학적 증명**이 필요합니다:

- Private Input을 공개하지 않고도 증명 가능
- 타원곡선 암호학을 사용하여 안전하게 증명

### 2. Groth16 프로토콜의 요구사항

Groth16 프로토콜은 **페어링(Pairing)**이라는 수학적 기법을 사용합니다:

```
검증 공식: e(pi_a, pi_b) = e(alpha, beta) * e(publicInput, gamma) * e(pi_c, delta)
```

이 공식을 검증하려면:

- **pi_a**: G₁ 그룹의 점 필요
- **pi_b**: G₂ 그룹의 점 필요
- **pi_c**: G₁ 그룹의 점 필요

**세 점이 모두 있어야** 검증이 가능합니다!

### 3. 각 점의 역할 분담

```
┌─────────────────────────────────────────────────┐
│  Proof 생성 과정                                 │
├─────────────────────────────────────────────────┤
│  1. Private Input 준비                          │
│     - 정부 DB 특징값                             │
│     - 카메라 특징값                              │
│     - 머클 경로                                  │
│                                                  │
│  2. 제약 조건 검증                                │
│     - 얼굴 유사도: distance <= threshold        │
│     - 머클 루트 계산: computedRoot = hash(...)   │
│                                                  │
│  3. 다항식 변환                                  │
│     - 모든 제약 조건 → 다항식                    │
│                                                  │
│  4. 타원곡선 변환                                │
│     ┌────────────────────────────────────────┐  │
│     │ pi_a: 입력값과 다항식의 관계            │  │
│     │ pi_b: 연산 과정의 암호화                │  │
│     │ pi_c: 일관성 검증 (pi_a + pi_b 결합)   │  │
│     └────────────────────────────────────────┘  │
│                                                  │
│  5. Proof 완성                                   │
│     - pi_a, pi_b, pi_c의 조합 = Proof           │
└─────────────────────────────────────────────────┘
```

### 4. 서버 검증 과정

서버는 이 세 점을 사용하여 검증합니다:

```typescript
// 서버 검증 코드 (의사코드)
const isValid = verifyProof({
  pi_a: proof.pi_a, // 첫 번째 증명 조각
  pi_b: proof.pi_b, // 두 번째 증명 조각
  pi_c: proof.pi_c, // 세 번째 증명 조각
  publicInput: "머클루트", // 공개 입력값
});

// 검증 공식:
// e(pi_a, pi_b) = e(alpha, beta) * e(publicInput, gamma) * e(pi_c, delta)
//
// 이 공식이 성립하면:
// ✅ 모든 제약 조건이 만족되었음
// ✅ Private Input이 올바름
// ✅ 연산 과정이 정확함
```

## 비유로 이해하기

### 비유 1: 3단 자물쇠

```
pi_a = 첫 번째 자물쇠
  └─ "나는 올바른 키를 가지고 있다"는 증거

pi_b = 두 번째 자물쇠
  └─ "나는 모든 단계를 올바르게 수행했다"는 증거

pi_c = 세 번째 자물쇠
  └─ "첫 번째와 두 번째가 서로 일치한다"는 증거

세 자물쇠가 모두 열려야 문이 열림 (검증 성공)
```

### 비유 2: 3단 증명서

```
pi_a = 신분증
  └─ "나는 누구인가" (입력값 증명)

pi_b = 학위증
  └─ "나는 무엇을 할 수 있는가" (연산 능력 증명)

pi_c = 통합 증명서
  └─ "신분증과 학위증이 같은 사람의 것" (일관성 증명)

세 증명서가 모두 있어야 최종 인증 (검증 성공)
```

## 실제 사용 예시

### 우리 프로젝트에서의 사용

```typescript
// 1. Proof 생성 (클라이언트)
const proof = await FaceVerificationProgram.prove(
  root,        // Public Input: 머클 루트
  features,    // Private Input: 정부 DB 특징값, 카메라 특징값
  merklePath   // Private Input: 머클 경로
);

// 2. Proof 구조 (snarkjs 형식인 경우)
{
  pi_a: ["x1", "y1", "z1"],           // 얼굴 유사도 + 머클 루트 계산의 첫 번째 증명
  pi_b: [["x1", "y1"], ...],          // 모든 연산 과정의 두 번째 증명
  pi_c: ["x1", "y1", "z1"]           // 일관성 검증의 세 번째 증명
}

// 3. 서버 검증
const isValid = await verificationKey.verify(publicInput, proof);
// → pi_a, pi_b, pi_c를 사용하여 검증
```

## 핵심 정리

### pi_a, pi_b, pi_c가 Proof에 들어가야 하는 이유:

1. **수학적 필수성**

   - Groth16 프로토콜의 검증 공식에 필요
   - 세 점이 모두 있어야 검증 가능

2. **보안성**

   - Private Input을 공개하지 않고도 증명 가능
   - 타원곡선 암호학을 사용하여 안전

3. **역할 분담**

   - pi_a: 입력값 증명
   - pi_b: 연산 과정 증명
   - pi_c: 일관성 증명

4. **검증 가능성**
   - 서버는 이 세 점만으로 모든 것을 검증 가능
   - Private Input을 알 필요 없음

## 주의사항

### o1js (Pickles) 형식의 경우

o1js는 Pickles 형식을 사용하므로:

- `pi_a`, `pi_b`, `pi_c`가 직접 보이지 않음
- 대신 `proof` 문자열 안에 암호화되어 저장됨
- 서버에서 o1js로 검증하면 내부적으로 사용됨

**하지만 수학적으로는 동일합니다!**

- Pickles 형식도 내부적으로는 pi_a, pi_b, pi_c를 사용
- 단지 직렬화 방식이 다를 뿐

## 결론

**pi_a, pi_b, pi_c는:**

- ZK-SNARK Proof의 **핵심 구성 요소**
- **세 점의 조합**이 "모든 조건이 만족되었다"는 증명
- **반드시 Proof에 포함**되어야 서버에서 검증 가능
- 각각 **다른 역할**을 담당하여 **완전한 증명** 구성

**Proof에 들어가야 하는 이유:**

- 수학적 필수성 (Groth16 프로토콜 요구사항)
- 보안성 (Private Input 보호)
- 검증 가능성 (서버 검증에 필요)
